# AGENTS.md

## 基本方針

- プレースホルダーや未実装 `TODO` を残したままタスクを完了しない
- コード変更後はビルド・リンター・テストを実行し、エラーを自己修復してから完了を報告する
- 依頼スコープ外のファイルは変更しない。変更が必要な場合は事前に確認する
- ユーザーへの説明・報告はすべて日本語で行う

## タスク開始時

1. 目的・スコープ・完了条件をユーザーと合意してから着手する
2. 関連ファイルを先に読み込み、既存の設計意図を把握する
3. 不明点は作業開始前にまとめて質問する（作業中に何度も中断しない）

## タスク完了時

1. 変更内容のサマリーを日本語で報告する（何を・なぜ・どう変えたか）
2. 作業ログセクションに今回の作業内容を追記する
3. 以下を実行してコミットする

```bash
git add .
git commit -m "<type>(<scope>): <変更内容の要約>"
```

**type 一覧:** `feat` / `fix` / `refactor` / `style` / `test` / `docs` / `chore` / `perf`

## コーディング規約

- `any` の使用は原則禁止（`unknown` + 型ガードを使う）
- シークレット・APIキーはコードにハードコードしない（`.env` で管理）
- ユーザー入力はサーバーサイドでバリデーションする
- 1ファイル・1コンポーネントの責務は1つ
- 既存のコードスタイル・命名規則・フォルダ構造に従う

## セキュリティ

- `.env` はコミットしない（`.gitignore` に追加済みか確認する）
- 保護されたルート・APIエンドポイントには必ず認証チェックを実装する

---

## 作業ログ

> エージェントはタスク完了時に必ずここに追記すること。
> 形式: `## YYYY-MM-DD <作業タイトル>`

<!-- ↓ここから追記 -->

## 2026-06-18 Misskey Drive 一括エクスポート機能の実装

MisskeyのDriveファイルをフォルダ構造を保ったままZIP圧縮してエクスポートするシステムを新規実装しました。
- NestJS APIサーバーの構築
- BullMQを使用した非同期ジョブ実行環境の整備
- SQLiteを使用したエクスポート進行状況の永続化
- Cloudflare R2（S3互換）を用いたZIP保管、プレーススルーでのアップロードおよびSigned URLの発行
- ダウンロード時に期限を7日間自動延長し、最大30日間生存するライフサイクル管理の実装
- 6時間ごとに期限切れファイルを自動削除するクリーンアップサービスの追加
- モダンなグラスモーフィズムデザイン of ダッシュボードUIの提供

## 2026-06-19 実行中状態のままスタックしたバックアップジョブのクリーンアップ対応

サーバー再起動などによってデータベース上に実行中（`processing`/`queued`/`uploading`）のまま残留していたジョブを停止（`failed`）状態に更新し、再発防止策を実装しました。
- データベース上のスタックしていた古いジョブステータスを手動で `failed` に変更し、実行中のスタックを解消
- `DatabaseService` の `onModuleInit` 時に、実行中ステータスのまま残されている古いジョブを自動的に `failed` へリセットする `resetHangingJobs` 処理を実装

